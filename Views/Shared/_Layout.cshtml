<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Electronics Store</title>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

</head>
<body>
    <header>
        <div class="header-top-bar">
            <div class="container">
                <a href="#" class="location-link">Ho Chi Minh City</a>
                <div class="user-links">
                    @Html.ActionLink("Lịch sử mua hàng", "Index", "Orders")
                    <a href="@Url.Action("Cart", "Home")" class="cart-link">
                        <i class="fa fa-shopping-cart"></i> Cart
                    </a>
                    @Html.ActionLink("Đặt hàng", "Index", "Checkout")
                    @Html.ActionLink("Chi tiết đơn", "Detail", "Orders", new { id = 1 }, null)
                    @Html.ActionLink("Đăng nhập / Đăng ký", "Login", "Home")

                </div>
            </div>
        </div>

        <div class="header-main">
            <div class="container">
                <a asp-controller="Home" asp-action="Index" class="logo">Electronics</a>

                <form class="search-bar" action="/Home/Category">
                    <input type="text" name="id" placeholder="Search for products..." />
                    <button type="submit">&#128269;</button>
                </form>

                <div class="hamburger-menu">&#9776;</div>
            </div>
        </div>

        <nav class="header-nav-bar">
            <div class="container">
                <a href="@Url.Action("Index", "Home")">Home</a>

                <a href="@Url.Action("Category", "Home", new { id = "Laptops" })">Laptops</a>
                <a href="@Url.Action("Category", "Home", new { id = "Phones" })">Phones</a>
                <a href="@Url.Action("Category", "Home", new { id = "Accessories" })">Accessories</a>


            </div>
        </nav>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <footer class="site-footer">
        <div class="container">
            <p>&copy; 2025 Cửa hàng điện tử. Mọi quyền được bảo lưu.</p>
            <p>
                Chuyên cung cấp điện thoại, laptop, phụ kiện chính hãng với giá ưu đãi.
                Cập nhật liên tục các chương trình khuyến mãi hot mỗi ngày!
            </p>

        </div>
    </footer>

    <script src="~/js/site.js" asp-append-version="true"></script>

    <header>
    </header>

    <main role="main">

    </main>

    <footer class="site-footer">
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

    <script src="~/Scripts/Site.js" asp-append-version="true"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>